# Plik ze zmiennymi środowiskowymi na czas budowania
# Next.js automatycznie wczyta ten plik podczas budowania
# Rzeczywiste wartości zostaną zastąpione przez zmienne środowiskowe z Railway podczas uruchamiania

# Domyślne zmienne dla Google OAuth (puste - będą nadpisane przez zmienne Railway)
GOOGLE_CLIENT_ID=dummy_id_for_build_time
GOOGLE_CLIENT_SECRET=dummy_secret_for_build_time

# Domyślne zmienne dla NextAuth (puste - będą nadpisane przez zmienne Railway)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=dummy_secret_for_build_time

# Domyślna baza danych (pusta - będzie nadpisana przez zmienne Railway)
DATABASE_URL=dummy_db_url_for_build_time 