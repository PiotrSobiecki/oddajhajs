# UWAGA: Ten plik jest używany tylko podczas procesu budowania.
# W środowisku produkcyjnym (Railway) te wartości MUSZĄ być nadpisane przez zmienne środowiskowe.
# Jeśli widzisz, że aplikacja używa tych wartości, oznacza to problem z konfiguracją Railway.

# Plik ze zmiennymi środowiskowymi na czas budowania
# Next.js automatycznie wczyta ten plik podczas budowania
# Rzeczywiste wartości zostaną zastąpione przez zmienne środowiskowe z Railway podczas uruchamiania

# Railway automatycznie dodaje cudzysłowy do zmiennych środowiskowych
# W kodzie obsługujemy to przez funkcję cleanEnv w src/lib/auth.ts

# Domyślne zmienne dla Google OAuth - W Railway muszą być rzeczywiste wartości!
# NIE UŻYWAJ TYCH WARTOŚCI W PRODUKCJI!
GOOGLE_CLIENT_ID=placeholder_id_for_build_time
GOOGLE_CLIENT_SECRET=placeholder_secret_for_build_time

# Domyślne zmienne dla NextAuth - W Railway muszą być rzeczywiste wartości!
# NIE UŻYWAJ TYCH WARTOŚCI W PRODUKCJI!
NEXTAUTH_URL=https://oddajhajs.org
NEXTAUTH_SECRET=placeholder_secret_for_build_time

# Domyślna baza danych - W Railway musi być rzeczywista wartość!
# NIE UŻYWAJ TYCH WARTOŚCI W PRODUKCJI!
DATABASE_URL=placeholder_db_url_for_build_time